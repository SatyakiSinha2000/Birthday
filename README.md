# Birthday Surprise 🎂

Interactive birthday website built with React + Vite.

## Run locally
```
npm install
npm run dev
```

## Deploy to GitHub Pages
```
npm run deploy
```
Then in GitHub → Settings → Pages, set source to `gh-pages` branch.
Site will be live at https://SatyakiSinha2000.github.io/Birthday/
